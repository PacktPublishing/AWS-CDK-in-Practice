import styled from "styled-components";

const AppContainer = styled.div`
  min-height: 100%;
`;

const MainSection = styled.section`
  display: flex;

  height: calc(100vh - 86px);
`;

export { AppContainer, MainSection };